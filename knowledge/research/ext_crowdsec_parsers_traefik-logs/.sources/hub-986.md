---
url: https://github.com/crowdsecurity/hub/issues/986
title: "[UnmarshalJSON] check if line startsWith `{`"
fetched: 2026-09-08
authority: ticket
---

Hub maintainer task (2024-02): consider checking line starts with `{` before UnmarshalJSON to suppress error spam when non-JSON lines hit JSON parsers (follow-up to crowdsec#2859).

Follow-up comment (2024-03): also add type-safety checks in JSON parsers; example warnings from caddy-logs when fields missing.

Traefik-logs YAML at research pin already includes `startsWith "{"` guard on JSON branch.
