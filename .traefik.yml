displayName: geoblock
type: middleware
import: github.com/david-garcia-garcia/traefik-geoblock
useUnsafe: true
summary: traefik plugin to block or allow requests based on geolocation
testData:
  # It doesn't appear to be possible to get the pilot plugin analyzer
  # to load local files. To prevent errors, the plugin is disabled here.
  # This will cause the plugin to not attempt to load the database file.
  mode: disabled
  # path is a full file path (operator seed). Omit it to use seeds/ via defaultFile + TRAEFIK_PLUGIN_GEOBLOCK_PATH.
  # databaseSources.seed.path: /data/geo/IP2LOCATION-LITE-DB1.IPV6.BIN
  # databaseSources.seed.databaseType: bin
  # databaseSources.default_ip2location.enabled: false
  # allowedCountries: [ "CH", "DE" ]
  # blockedCountries: [ "RU" ]
  # defaultAllow: false
  # allowPrivate: true
  # disallowedStatusCode: 403
  # allowedIPBlocks: ["66.249.64.0/19"]
  # blockedIPBlocks: ["66.249.64.0/24"]