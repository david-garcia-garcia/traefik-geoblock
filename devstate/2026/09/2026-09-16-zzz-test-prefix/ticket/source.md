Prefix every first-party Go package test file (`*_test.go`) with `zzz_` so tests cluster in the explorer and visually differentiate from production `.go` files. Keep the `_test.go` suffix so `go test` still discovers them (example: `bin_test.go` → `zzz_bin_test.go`). Open a pull request. Original ask: `/opd-workflow haz un PR que ponga a todos los tests el prefijo zzz_`. Scope is filename convention only; no product runtime behavior change.
