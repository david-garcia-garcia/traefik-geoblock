# Spec

1. [wrong] `design.md:54` "An `Open` that lands in the arming window bumps the generation, so the arm is skipped, the slot stays live, and `reclaim_reclaim` is logged after `reclaim_orphan`" — `pkg/reclaim/table.go:225` — the code does not order those two lines. `drop` sets `arming` and releases the mutex (`table.go:223-225`) *before* it logs orphan (`table.go:227`); a concurrent `Open` can take the mutex in that gap, get `reclaimed = true` from `bindLocked` (`table.go:178`), release it, and emit `reclaim_reclaim` / `reclaim_bind` (`table.go:124`) while the orphan line has not been written yet. Recorded order can be `reclaim, bind, orphan`. The ordering the design claims holds only for a bind that lands after the timer is armed, because that bind must acquire the mutex after `drop`'s second critical section, which is sequenced after the orphan log. `tasks.md:38` (5.3) asks for a test that asserts `reclaim_reclaim` after `reclaim_orphan`; the test written for it (`pkg/reclaim/table_test.go:1020`, `TestTable_BindWhileArmingReclaims`) calls `bindLocked` under the lock and asserts only `reclaimed` and the generation bump, so the ordering half of 5.3 is not covered — consistent with the guarantee not existing.
   → Scope the claim to what the change delivers: in `design.md:54` say the arming-window bind keeps the slot live and logs `reclaim_reclaim`, without asserting it lands after `reclaim_orphan`, and drop the ordering clause from `tasks.md:38`. Keep the delta spec sentence about the *grace window* as is. If the ordering is actually wanted for the arming window too, it needs a barrier the bind path also observes (e.g. `drop` publishing "orphan logged" and `logBind` waiting on it for an `arming` slot) plus a test that drives the two goroutines through the window.
   Status: done
   Argument: `design.md` now states the opposite of the old claim — the arming-window bind is explicitly not ordered against the orphan line, with the reason (the bind takes the mutex `drop` released in order to log) and the contrast against a bind after the arm. Task 5.3 lost the ordering clause and says not to assert an order that cannot hold. The delta spec now names this as one of two stated exceptions rather than leaving it implied. No barrier was added: the ordering is not worth a handshake on the bind path for a sub-millisecond window.

2. [wrong] `openspec/changes/reclaim-dispose-determinism/specs/std_go_reclaim_context-lease/spec.md:32` "so for one incarnation `reclaim_orphan` always precedes `reclaim_dispose` … at every grace duration including zero" — `pkg/reclaim/table.go:293` — the `Reset` path is not ordered against an in-flight orphan. `drop` marks `arming`, unlocks, and then logs orphan (`table.go:223-227`); a `Reset` that runs in that gap takes the slot off the map, clears `arming`, bumps the generation (`table.go:281-291`), cancels, waits on `closed` and logs `reclaim_dispose` (`table.go:294-298`) with nothing waiting for the orphan line, so the recorded order for that incarnation can be `dispose` then `orphan`. The `fire` side of the sentence is delivered (a timer can only be armed after the orphan log, and the zero-grace path calls `fire` on the same goroutine after logging orphan); only dispose-via-`Reset` escapes it. `Reset` is test-only, but the requirement states the guarantee unconditionally, and `Reset` racing a holder cancel is exactly what `TestTable_ResetRacingOpenClosesEveryValue` and the `t.Cleanup(Reset)` in `pkg/dbwrappers/reclaim_test.go` do.
   → Qualify the sentence in the delta requirement: the orphan-before-dispose guarantee covers the dispose that ends grace (`fire`, including zero grace); `Reset` cancels every mapped incarnation immediately and may emit `reclaim_dispose` before an orphan line that a concurrent `drop` has not yet written. Nothing in the code needs to change for this.
   Status: done
   Argument: The MODIFIED requirement now scopes the guarantee to "the `reclaim_dispose` that ends grace" and states the `Reset` exception explicitly. Same qualification added to the devdocs gotcha, so the next reader of the packet sees it too. No code change, as recommended.

3. [wrong] `devstate/2026/09/2026-09-08-fix-reclaim/explore.md:35` "`fire` and `Reset` will call `stopValue(value)` **synchronously** after `cancel()` … and the per-slot `waitCtx(life)` goroutine goes away … removes one goroutine per incarnation" — `pkg/reclaim/table.go:163` — this is the approach the human reversed, and the decision record still states it as the decision. The same file at line 59 asks for "a goroutine-count check that proves the per-slot goroutine is gone". The code does the opposite and correctly so: the lifetime goroutine stays and closes `e.closed` after `stopValue` (`table.go:163-167`), `fire` and `Reset` wait on it (`table.go:261`, `table.go:297`), and `TestTable_GoroutinesReturnToBaseline` (`table_test.go:1047`) checks a return to baseline rather than the absence of that goroutine. The reversal is recorded further down the same file (`explore.md:73-75`), so the artifact contradicts itself; `proposal.md`, `design.md` (Context, Non-Goals, and the rejected alternative), `tasks.md:0.1`/`1.4`, the delta spec, and `knowledge/devdocs/std_go_reclaim.md` all state the additive shape correctly. No requirement anywhere describes the abandoned approach. (`devstate/` is outside the pinned diff, so this is an artifact fix, not a code one.)
   → Rewrite the "Root cause 1" decision paragraph in `explore.md` to the shape that was implemented — the lifetime goroutine and `waitCtx` stay, the slot gains a `closed` channel that the goroutine closes after `Close()`, and `fire` / `Reset` wait on it before logging dispose — and reword line 59 to "a goroutine-count check that proves no goroutine outlives the key". Leave the resolved question at line 73 as the audit trail.
   Status: done
   Argument: Both rewritten as recommended, and the abandoned approach is kept as one sentence pointing at `design.md`, where it lives as the rejected alternative. The Root cause 3 decision was also corrected in the same pass: it described the generation check that the performance axis proved strands a slot.

4. [judgement] `openspec/changes/reclaim-dispose-determinism/specs/std_go_reclaim_context-lease/spec.md:4` "When an incarnation ends (grace elapsed while orphaned, `Reset`, or a lost create race), the table SHALL cancel the incarnation lifetime, then wait until the stored value's `Close()` has returned, and only then emit `reclaim_dispose`" — `pkg/reclaim/table.go:145` — the lost-create-race path cancels and calls `stopValue(created)` inline and emits no `reclaim_dispose` at all, which is right (the value was never stored, and the baseline "Lost create race" scenario asks only that the lifetime be canceled and the value not stored). As written the requirement reads as if that path must log dispose too, and it has no scenario, so a future reader could implement a fourth dispose line. Judgement, not a defect: the ordering the sentence demands is trivially satisfied there.
   → Split the sentence: keep the enumeration for "cancel then close before reporting", and state the dispose clause only for the two paths that emit it (grace expiry and `Reset`), noting the lost create closes inline and logs nothing.
   Status: done
   Argument: Split as recommended. The requirement now states the dispose clause for grace expiry and `Reset` only, and says a lost create race closes inline and emits nothing, with the reason (the value was never stored).
